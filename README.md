# SAYC

```shell
npx hardhat node
npx hardhat deploy --network localhost
npx hardhat run scripts/get_deployed_contracts.js --network localhost
npx hardhat run scripts/create_batch_registerd_users.js --network localhost
```
